# language: c++
# compiler:
#   - clang++
#   - g++
# image: ubuntu
image: node:latest
before_script:
  - echo `pwd`
  - apt-get update -qq && apt-get install -y liblzma-dev libbz2-dev libz-dev git g++ build-essential cmake

my_deammeth_test:
  script: 
    #- find . 
    - make -f Makefile
  
  
