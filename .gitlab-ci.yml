# language: c++
# compiler:
#   - clang++
#   - g++
image: ubuntu

before_script:
  - apt-get update -qq && apt-get install -y liblzma-dev libbz2-dev libnlopt-dev libz-dev git g++ build-essential
  - git clone git://github.com/samtools/htslib.git
  - cd htslib;make;cd ..
  
my_deammeth_test:
  script: 
    - make -f Makefile
  
  
